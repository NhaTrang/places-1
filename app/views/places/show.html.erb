<p id="notice"><%= notice %></p>

<% @place.photos.each do |photo| %>
	<p>
		<img src="<%= photos_show_path("#{photo.id}") %>" height="600px" width="650px" >
	</p>
<% end %>

<p>
  <strong>Formatted address:</strong>
  <%= @place.formatted_address %>
</p>


<%= link_to 'Back', places_path %>
